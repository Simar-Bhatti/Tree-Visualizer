# 🌳🔗 Tree & Graph Algorithm Visualizer 

An interactive **Algorithm Visualizer** built using **vanilla JavaScript**, designed to visually demonstrate **tree traversals** and **graph algorithms** with step-by-step animations.

This project focuses on **clear architecture, clean state management, and algorithm visualization**, without relying on heavy frameworks.

---

## 📌 Project Overview

The Tree & Graph Algorithm Visualizer allows users to:

- Build **binary trees** using level-order input
- Animate **tree traversals** (Inorder, Preorder, Postorder)
- Build **graphs** using edge lists
- Visualize **graph algorithms** such as:
  - BFS (Breadth-First Search)
  - DFS (Depth-First Search)
  - Dijkstra’s Algorithm
  - A* Search

The project emphasizes **separation of concerns**, **modular JavaScript**, and **asynchronous animation control**.

---

## ✨ Features

### 🌳 Binary Tree
- Level-order tree construction
- Handles `null` nodes
- SVG-based edge rendering
- Animated traversals:
  - Inorder
  - Preorder
  - Postorder

### 🔗 Graph Algorithms
- Unweighted & weighted graphs
- Circular graph layout
- Canvas-based rendering
- Algorithm visualizations:
  - BFS
  - DFS
  - Dijkstra
  - A*
- Step-by-step animation using async delays
